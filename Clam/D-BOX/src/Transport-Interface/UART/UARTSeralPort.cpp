//
// Created by gabri on 7/23/26.
//
#include <Transport-Interface/UART/UARTSeralPort.h>
#include <iostream>
#include <stdexcept>
namespace DBOXProtocol {
    // int UARTSerialPort::start() {
    //     if (Discovery == nullptr) {throw std::runtime_error("Discovery is a null pointer");}
    //     return Discovery->discover();
    // }
    // int UARTSerialPort::end() {
    //     if (Discovery == nullptr) {throw std::runtime_error("Discovery is a null pointer");}
    //     return Discovery->stop();
    // }
}