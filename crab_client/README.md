# crab-client

Standalone Python client codebase for talking to the Coral multiplexor.

Layout:

- `src/crab_client`: importable package
- `examples`: local demo entrypoints

This package intentionally does not talk to firmware directly.
